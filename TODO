TODO
----

1. Paste just values or just format of yanked cells.
   Paste cells with and without FIXED references.

2. multiple worksheets!!

3. filter rows by multiple criteria.

4. scim_scripting. Use LUA to allow scripting and plugin support.

5. isolate UI code (CURSES) from the rest of the code for future
implementations in other gui's.

6. status bar format customized by user.
can show count of cells and sum of values, when a range is selected..
*this should be done with a script.

7. chain cells with equations into a DOUBLE linked list or dependency tree
Have a top level eval, eval and UPDATE all lower nodes
This is also neccesary for UNDO mods of cells that has equations on them!!!

8. Store user defined variables with "define" token into .sc file.

9. Use UTF-8 encoding and display any Unicode char.
Change curses calls to cursesw calls.

10. restore fill funcionality

11. restore abbrev funcionality ?

12. multiplier with '.' command ?

