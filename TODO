TODO
----

1. multiple worksheets!

2. filters rows (multiple criteria)

3. Import of XLSX format spreadsheets

4. isolate UI code (CURSES) from the rest of the code for future implementation in other gui's.

5. status bar format customized by user.
   can show count of cells and sum of values, when a range is selected..

6. mapping: special caracters should be mapped.

7. chain cells with equations into a DOUBLE linked list or dependency tree
Have a top level eval, eval and UPDATE all lower nodes
This is also neccesary for UNDO mods of cells that has equations on them!!!

8. multiplier with '.' command ?
